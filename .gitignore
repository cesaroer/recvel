# macOS
.DS_Store

# Xcode user/local state
xcuserdata/
*.xcuserstate
DerivedData/
build/
*.moved-aside
*.xcscmblueprint

# Swift Package Manager / tooling
.build/
.swiftpm/

# Local assistant/tool configuration
.claude/settings.local.json

# Large local reference videos are useful during design, but should not be pushed
# to GitHub without Git LFS. Extracted frames/images remain versionable.
*.mp4
*.mov
